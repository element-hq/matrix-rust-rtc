{
  "name": "matrix-rtc-web-demo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Call page over the wasm bindings + livekit-js: a human demo UI and, with ?test=1, the Playwright-drivable web interop peer.",
  "license": "AGPL-3.0-only OR LicenseRef-Element-Commercial",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "livekit-client": "2.22.1",
    "matrix-js-sdk": "42.2.0",
    "matrix-rtc-wasm": "file:.."
  },
  "devDependencies": {
    "vite": "^6.3.6"
  }
}
